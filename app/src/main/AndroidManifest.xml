<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.italosvieira.dudu">

    <application
            android:allowBackup="true"
            android:icon="@drawable/logo_blue"
            android:label="@string/app_name"
            android:roundIcon="@drawable/logo_blue"
            android:supportsRtl="true">
        <activity
                android:name=".SplashScreenActivity"
                android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".CadastrarVacinaActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".CadastrarMedicamentoActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".DetalharPetActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".CadatroContaActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ConfiguracaoActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".BaseActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ListarPetsActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".LoginActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".CadastrarPetActivity" android:theme="@style/AppTheme"/>
    </application>

</manifest>